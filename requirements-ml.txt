# Optional: ED-LSTM training (services/ml_predictor/train.py) and loading .keras for local inference.
# Install after deploy deps: pip install -r requirements-deploy.txt -r requirements-ml.txt

tensorflow==2.20.0
